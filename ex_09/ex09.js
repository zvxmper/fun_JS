/* arrays o arreglos  

un array es una estructura de datos que se utiliza para almacenar coleccion ordenada de elementos.
estos elementos pueden ser cadenas de textos, variaos numericos como: enteros, decimales, valores booleanos, entre otros, pueden ser tambien un conjunto e inclusive otros arrays,

*/

let numeros = []; // declaracion de un array vacio 

numeros = [15, 80, 650, 50.43, -10]; // se guarda en el array una coeccion numerica de tipo decimal o entero 
console.log(numeros);// se imprime por consola el array  
console.log(numeros[3]); // se imprimer por consola el 4 elemnto que pertenece al indice 3 